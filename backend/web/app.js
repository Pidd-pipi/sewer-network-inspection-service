fetch('/healthz').then(r=>r.json()).then(x=>document.body.insertAdjacentHTML('beforeend','<p>'+x.status+' / '+x.service+'</p>'));fetch('/api/inspections').then(r=>r.json()).then(xs=>document.body.insertAdjacentHTML('beforeend','<ul>'+xs.map(x=>'<li>'+x.manhole+': '+x.status+' <button data-id="'+x.id+'">Resolve</button></li>').join('')+'</ul>')).then(()=>document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>fetch('/api/inspections/'+b.dataset.id+'/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:'resolved'})})));
